/**
 * @file Typed PDF Viewer Engine events.
 * @description Carries lifecycle snapshots and structured outcomes without a
 * framework event bus dependency.
 */

export type { PdfViewerEvent, PdfViewerListener } from './types'
